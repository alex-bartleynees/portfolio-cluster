apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: redis-tls
  namespace: redis
spec:
  secretName: redis-tls
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
  dnsNames:
    - redis.alexbartleynees.com
  usages:
    - digital signature
    - key encipherment
    - server auth
    - client auth

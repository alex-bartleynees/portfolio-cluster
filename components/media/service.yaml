apiVersion: v1
kind: Service
metadata:
  name: media-tailscale
  namespace: media
spec:
  type: ClusterIP
  ports:
    - port: 8096
      targetPort: 8096
      protocol: TCP
      name: http
---
apiVersion: v1
kind: Endpoints
metadata:
  name: media-tailscale
  namespace: media
subsets:
  - addresses:
      - ip: 100.98.211.116
    ports:
      - port: 8096
        name: http

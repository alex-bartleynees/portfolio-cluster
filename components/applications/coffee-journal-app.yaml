apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: coffee-journal-app
  namespace: argocd
spec:
  project: default
  sources:
    - repoURL: https://github.com/alex-bartleynees/coffee-journal.git
      path: deployment/helm-charts/coffee-journal-app
      targetRevision: HEAD
      helm:
        valueFiles:
          - values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: coffee-journal
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
